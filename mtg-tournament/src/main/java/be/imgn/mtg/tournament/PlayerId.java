package be.imgn.mtg.tournament;

/// A unique identifier for a tournament player.
///
/// PlayerId is an opaque interface — the tournament does not interpret its contents.
/// Implementations must correctly implement [Object#equals] and [Object#hashCode].
public interface PlayerId {}
